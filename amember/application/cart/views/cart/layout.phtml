<?php
if (empty($title)) $title = ___('Shopping Cart');
$this->setLayout('layout.phtml');
$this->headLink()->appendStylesheet($this->_scriptCss('cart.css'));
$this->headScript()
        ->appendFile(REL_ROOT_URL . '/application/default/views/public/js/jquery/jquery.js')
        ->appendFile($this->_scriptJs('cart.js'));
?>
<div class="am-cart">
    <div class="am-cart-main">
        <div>
        <?php if(!empty($header)) : ?>
            <h2><?php p($header) ?></h2>
            <?php if (!empty($description)) : ?>
            <p class="am-cart-category-desc"><?php p($description) ?></p>
            <?php endif; ?>
        <?php endif;?>
        <div class="am-cart-error">
            <?php include $this->_script('_error.phtml'); ?>
        </div>
        <?php echo $content ?>
        </div>
    </div>
    <div class="am-cart-sidebar">
    <?php // output blocks enveloped into a <div>
        echo $this->blocks('cart/right', '<h2 id="%3$s-head">%2$s</h2><div class="am-block block" id="%3$s">%1$s</div>');
    ?>
    </div><!-- div#navigation -->
</div><!-- div#cart -->